Use the to-ceo skill. Write the recommendation. Context: the public API caps every key at 1,000 requests per minute and has since 2023. Two enterprise customers now break that cap during nightly syncs and have both asked for exemptions. Per-customer exemptions are config entries nobody owns; they drift and generate support tickets, and they stop being manageable past about ten. There are 40 keys currently exempted. Moving to per-plan limits puts the ceiling in the plan, which billing already tracks; migrating the 40 keys takes about a day. Ravi owns this decision and the enterprise renewal needs an answer by the 20th. Keep it short. Output only the recommendation.
